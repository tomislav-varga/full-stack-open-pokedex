name: Hello World

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Hello World
        run: echo "Hello World!"
      - name: Now it is
        run: date
      - name: Directory content
        run: ls -l